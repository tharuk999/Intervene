package com.interactout.model;

public enum InterventionType {
    TAP_DELAY,
    TAP_PROLONG,
    TAP_SHIFT,
    TAP_DOUBLE,
    SWIPE_DELAY,
    SWIPE_DECELERATE,
    SWIPE_REVERSE,
    SWIPE_MULTI_FINGER
}
